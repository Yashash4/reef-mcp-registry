__pycache__
*.pyc
*.pyo
*.pyd
.pytest_cache
.mypy_cache
.ruff_cache
.coverage
htmlcov
.venv
venv
env
.git
.gitignore
.dockerignore
Dockerfile
*.log
data/
keys/
.DS_Store
Thumbs.db
# NOTE: README.md is NOT excluded — pyproject.toml's `readme = "README.md"`
# means setuptools needs it during `pip install -e .`, and the Dockerfile
# copies it explicitly. Excluding it here breaks the build.
